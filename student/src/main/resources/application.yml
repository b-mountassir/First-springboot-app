spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/demo"
    username: postgres
    password: Iamthe-best-boy2!
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
        format_sql: true